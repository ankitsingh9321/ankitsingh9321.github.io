# Gazab GFX Portfolio

Open `index.html` in a browser.

The site uses a system/local SF Pro font stack (`SF Pro Display`, `SF Pro Text`, then Apple/system fallbacks) so no external font request is required.

Edit client testimonial copy inside `index.html` where the three `Client testimonial` placeholders appear.
